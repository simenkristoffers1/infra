import { css } from 'lit';

export const chatStyles = css`
  .chat-container {
    max-width: 400px;
    min-width: 400px;
    height: 100%;
    border-left: var(--border-width-sm) solid var(--border-primary);
  }
`;
